//Task 4.1

// Set radius
const radius = 5.678;

//Calculation of the area of the circle
let circleArea = Number(((Math.PI * Math.pow(radius, 2)).toFixed(2)));

//Show result in console
console.log("The area of the circle is :", circleArea);