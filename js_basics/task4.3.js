//Task 4.3

//Creation of radius and height variables
const radius = 3.456;
const height = 7.89;

// Calculation of the volume of the cylinder
const volume = Number((Math.PI * (radius ** 2) * height).toFixed(2));

// output the result
console.log("Cylinder volume is:", volume);